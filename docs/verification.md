# Verification

## Commands (run from `android`)
1. Build debug APK:
   - `./gradlew assembleDebug`
2. Run unit tests:
   - `./gradlew test`

## Expected results
- Build completes with no errors.
- Unit tests pass.
